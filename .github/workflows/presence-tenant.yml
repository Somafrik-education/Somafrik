name: Presence tenant GP-015

on:
  pull_request:
    branches: [develop]
    paths:
      - "backend/lib/presenceSchoolScope.js"
      - "backend/lib/presenceSchoolScope.test.js"
      - "backend/lib/presenceTenant.guard.test.js"
      - "backend/lib/presenceTenant.http.pg.test.js"
      - "backend/db/postgresRepository.js"
      - "backend/db/pedagogyPgStore.js"
      - "backend/scripts/verify-presence-tenant.js"
      - "backend/server.js"
      - "package.json"
      - ".github/workflows/presence-tenant.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  presence-tenant:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres
      NODE_ENV: test
      JWT_SECRET: ci-test-secret-with-enough-length-for-production-checks
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.12.0"
          cache: npm
          cache-dependency-path: |
            package-lock.json
            backend/package-lock.json
      - name: Install dependencies
        run: |
          npm ci
          npm ci --prefix backend
      - name: Verify Presence tenant GP-015
        run: npm run verify:presence-tenant
