# Cible de l'API Express pour le proxy Vite en développement.
# Par défaut: http://localhost:5000
VITE_API_TARGET=http://localhost:5000

# Marketplace (préproduction) — route /marketplace, placeholder sans API.
# Laisser à false tant que le module n'est pas validé.
VITE_ENABLE_MARKETPLACE=false
