# `@/design-system` — D2.1 → D2.7

Point d’entrée : `import { Button, Modal, Table, EntityListShell, useToast, … } from "@/design-system"`.

Documentation : [`docs/ux/design-system/`](../../../docs/ux/design-system/README.md)

- **D2.7** — patterns `entity-list` (`EntityListShell`, `EntityListSearch`, `EntityListTable`, `EntityListForbidden`) ; `EntityPage` compose `ListLayout`
- **D2.6** — stabilisation runtime : Toast + Confirm montés depuis le DS ; Modal / Table livrés ; re-exports `components/ui/*`
- Nouveaux écrans : importer **uniquement** `@/design-system`
- Attendre validation CTO avant D3.2b (Liste Classes) et D3.3 (Enseignants)
