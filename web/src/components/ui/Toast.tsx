/**
 * @deprecated Importer depuis `@/design-system` (D2.6).
 * Re-export de coexistence — même React context que le provider DS.
 */
export {
  ToastProvider,
  useToast,
  type ToastContextValue,
  type ToastTone,
} from "../../design-system/feedback/Toast";
